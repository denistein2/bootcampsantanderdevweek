spring:
  datasource:
    url: jdbc:h2:file:C:/Users/Denis/test
    driver-class-name: org.h2.Driver

    username: sdw2023
    password:
  jpa:
    show-sql: true
    open-in-view: false
    database-platform: org.hibernate.dialect.H2Dialect

    hibernate:
      ddl-auto: create
      properties:
        hibernate:
          format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        trace: false
        web-allow-others: false
